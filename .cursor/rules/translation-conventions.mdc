---
description: Translation Conventions — 文档翻译与本地化约定
alwaysApply: true
---

# Translation Conventions

- 优先保证可用性和一致性，其次再追求文案润色。
- 尽量保留原有结构、链接、安装命令、代码块和目录组织不变。
- 不翻译命令、路径、环境变量、配置键、slash commands、文件名和真实目录名。
- 标题层级、表格结构、`details/summary` 折叠块、HTML 标签应尽量原样保留，避免破坏渲染。
- 若翻译标题可能导致目录或内部跳转失效，应补充显式锚点，保证旧链接仍可用。
- 目录链接文本可以翻译，但链接目标必须继续指向正确锚点或文件路径。
- 外部链接的说明文字可以本地化，但 URL 本身不要修改。
- 若需将本仓库在 GitHub 上的展示地址（如 `README` 中的 `git clone`、徽章、Star History、安全披露、讨论区入口等）从上游改为你维护的 fork 或镜像，应统一替换为当前仓库地址，并同步修正克隆后的目录名等配套说明；作者博客、第三方 skills 仓库等其他仓库链接保持不变。
- 同一术语全篇保持一致。`Claude Code`、`MCP`、`Hooks`、`Skills`、`Subagents` 等优先保持统一写法。
- 已成为产品概念或用户会直接操作的词，优先保留英文或采用中英混写，避免生硬直译。
- 用户实际会输入的命令、事件名、配置项名、权限模式名应保留精确原文。
- 只翻译说明文字，不擅自修改版本号、兼容性、统计数字、功能承诺等项目事实。
- 不擅自新增原文没有的结论、能力描述或承诺。
- 长文档优先先通读结构，再整体翻译；可一次性改完整个文件时，尽量不要零碎拆改。
- 优先保证整页术语、语气和格式一致，再追求单句表达的“更漂亮”。
- 可适度意译以保证中文自然，但不要改变原文逻辑、顺序和约束条件。
- shell 注释、说明性文字可以翻译，但必须保证命令复制后仍可直接执行。
- JSON、YAML、配置示例中的键名、代码标识符和可执行内容默认不翻译。
- 对徽章、图片、嵌入 HTML、Mermaid、表格和代码围栏要保守处理，优先保证渲染和可复制性。
- 如果仓库已有中文术语或既有翻译风格，应优先与现有文档保持一致，不另起一套说法。
- 常见错误包括：翻译命令导致无法执行、翻译文件名导致路径失效、只改标题不处理锚点、同一术语多种译法混用。
- 翻译完成后，至少自查以下内容：是否残留关键英文段落、术语是否统一、链接/锚点是否有效、表格和代码块是否正常渲染。
